import React from 'react';
import './Loader.scss'; // Создайте этот файл для стилей

const Loader = () => {
    return (
        <div className="loader">
            <div className="loader__spinner"></div>
        </div>
    );
};

export default Loader;
